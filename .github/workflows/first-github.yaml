name: RK github workflow
on:
  push:
    branches:
      - main
jobs:
  RK-Example_Job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Checkout the repo"
      - name: Checkout Repo
        uses: actions/checkout@v4
      - run: printenv
      - run: echo "done successfully"
